# 科研系统 1.0 / v3.0.0 架构

## 1. 设计目标

科研系统 1.0 在既有科研闭环之上优先解决长期陪伴、多工作区课题、可执行计划、可回存 AI 协作、实用游戏系统与本地个性化等问题：

1. 计划可以随科研状态随时变化；
2. 每次学习必须留下可检查交付；
3. 复盘只能基于本人交付的受控关键文本；
4. 游戏系统必须映射真实科研能力，不另造一套空转数据；
5. 版本升级不能破坏科研资料和个性化。
6. 首页保持为轻量山门，只承担双检索和高频功能分流；
7. 知识资产可以脱离本软件，以 Markdown、JSON 和原附件一键导出。
8. 修炼里程碑与当天行动使用不同数据和验收标准。
9. 专业模块由每个人的动态工作区决定；EG、LAMMPS、数据集、ML、MD 与 COMSOL 只是可改名、可停用的默认模板。
10. 复盘、综合秘境和境界雷劫分成三个不同场景。
11. 境界采用 39 个稳定键和逐阶不同的经验需求。
12. 规模化联机只建立可替换接口，默认不启用也不假装已经完成。
13. 游戏化必须支持自主选择、能力进展和真实科研意义，不用惩罚式断签或虚假宝箱驱动使用。
14. CMD 源码开发、EXE 正式使用、程序文件与个人数据彼此分离，更新程序不得覆盖科研资料。
15. 课题进展必须由可审查的成功标准、相关先例、证据闸门和决策记录构成，不能用在线时长或主观百分比冒充。
16. 个人科研节点与轻量联机中心分别提供可双击 EXE，普通成员只运行个人节点，中心管理员才运行联机中心。
17. 系统必须保存科研阶段、关键决策、失败转化、技能形成、成果和方向转折，成为跨阶段的生涯记忆，而不只是知识文件仓库。
18. 法器经济必须可理解、可核算且事务一致；开宗礼包应至少允许完成一次真实购买。
19. 山门动态应表达时间与长期陪伴感，同时遵守 `prefers-reduced-motion`，不能妨碍阅读和操作。
20. Android 客户端必须通过明确的 ResearchHub 通道工作，不复制个人节点数据库，不提供 JavaScript 原生桥；公网必须使用 HTTPS。
21. 导航顺序与可见性由研究者本人控制，同时保留稳定路由键、无障碍替代操作和永远可达的恢复入口。

旧版 `research_tracks` 与 `research_plan_items` 继续保留在数据库中，只作为兼容数据，不再出现在主导航。`/foundation` 会转到近期计划，避免升级时直接删除用户旧数据。

## 2. 本地优先与联机扩展

```text
个人节点（每人一份）
├─ FastAPI + Jinja2
├─ research_os.db
├─ uploads / deliveries / simulations
├─ 近期计划、课题推进、复盘、炼丹
└─ 可替换同步适配器（默认 disabled）
          │
          ├── legacy_hub ── 轻量同行会（兼容）
          └── cloud_v2 ──── 规模化云端（仅预留）
                           ├─ 身份与公开主页
                           ├─ 轻量资产
                           ├─ 个性化
                           ├─ 资料链接卡
                           └─ 版本包

Android APK
└─ 安全 WebView（禁用 JavaScript 与原生桥）
   └─ ResearchHub 登录与轻量协作界面
```

关闭模式不会新增队列、上传或拉取数据。论文、实验数据、图片、代码和交付附件不进入轻量中心。未来规模化接口通过稳定事件信封、后端协议和 OpenAPI 草案固定，详见 `docs/SCALE_READY_SYNC_INTERFACE.md`；真正上线前仍必须建设标准登录、多租户数据库、审计、限流与恢复体系。

轻量同行会的稳定性保护在客户端完成：

- 本地事务先提交，联机失败不回滚科研操作；
- 事件使用唯一 UUID，中心按 UUID 幂等处理；
- 自动同步超时 1.5 秒，临时错误采用指数退避与确定性抖动，并遵守中心 `Retry-After`；
- 连续 3 次失败后至少熔断 5 分钟，防止断网时每次操作都卡顿；
- 单事件 6 次失败或遇到认证/参数错误时进入人工恢复队列；
- 手动“检测地址与 Token”使用只读接口，不产生同步事件；
- 公网 URL 强制 HTTPS，HTTP 仅允许回环或私有局域网。

“保存并联机”会在同一次用户动作中保存经过验证的中心地址与 Token、生成幂等首次迁移事件、推送当前轻量状态并拉取中心结果。端到端测试会启动独立 Hub 进程，验证首次合并、自动同步、法器交易和重复事件保护。

### 2.1 科研生涯罗盘

知识条目解决“我保存过什么”，课题推进解决“当前课题怎样过证据闸门”，生涯罗盘解决“不同课题和阶段如何形成连续成长”：

- `settings` 保存当前科研阶段、阶段目标、主动边界、完成信号与复看日期；
- `career_moments` 保存关键决策、突破、失败转化、技能、成果、合作和方向转折；
- 唯一下一步按“活跃课题证据闸门 → 未完成近期任务 → 活跃课题 → 重新定义问题”选择；
- 数量只显示留下的证据，不自动等于科研质量；
- 生涯节点进入 JSON、开放知识库导出和完整备份；
- 阶段与方向允许随时修正，历史判断保留当时的理由与证据。

### 2.2 自定义导航

`settings.nav_layout` 使用稳定分组键和页面键保存布局，不保存 URL 或模板片段。标准化器负责：

- 去除未知分组、未知条目和重复条目；
- 保留合法的分组顺序、组内顺序和显示状态；
- 新版本出现新入口时，将其追加到默认分组；
- 动态工作区入口作为一个整体开关，具体工作区仍沿用 `workspaces.active` 与 `sort_order`；
- 即使隐藏设置页面入口，侧栏底部仍保留 `/settings#navigation-layout` 恢复链接。

桌面支持拖拽，触屏和键盘用户可使用上下移动按钮；持续动画不参与导航编辑。布局进入个性化 v7 包和轻量同步，但论文、附件与数据库仍不进入 Hub。

## 3. 近期计划

`services/prompt_builder.py` 从下列信息生成计划提示词：

- 当前目标；
- 尚未完成的近期任务；
- 最近交付及其关键文本；
- 最近一句状态记录。

大模型输出采用宽松 Markdown 协议：

```text
# 计划名
> 说明
## 修炼任务
- [进阶] 跨天成果 | 验收：可验证证据 | 工作区：工作区名称
## Day 1 | 主题
- [重点] 当天行动 | 45min | 交付：... | 关联修炼：跨天成果
```

`services/plan_import.py` 将跨天成果写入 `quests`，将当天行动写入 `study_plans` 与 `daily_missions`。二者通过 `daily_missions.quest_id` 可选关联。启用新计划时，旧计划只归档，不删除。

导入文本中的 XP、经验或修为全部忽略。`services/progression.py` 按统一时长档位计算每日奖励，并按三档难度计算修炼任务奖励。

## 4. 课题推进

`research_projects` 保存科学问题、价值、目标成果、成功标准、当前基础、现实约束、检索词、状态、工作区与目标日期。建立课题时不会生成虚假的完成度，而是创建五个可修改的证据闸门：

1. 问题与成功标准；
2. 先例与差距；
3. 方法与可行性；
4. 证据与可重复性；
5. 成果与边界。

`project_milestones` 将每个闸门保存为 `planned / active / passed / revise / stopped`。标记为 `passed` 时必须同时留下证据或决策理由；当前闸门通过后才激活下一闸门。`project_cases` 保存 Crossref 返回的公开书目信息以及本人对“直接基线、可借方法、反例/冲突、相邻启发”的判断；`project_updates` 保存推进、证据、卡点、失败和决策记录，以及唯一下一行动。

课题页把“定义完整度”和“证据进度”分开：前者只检查必要信息是否齐全，后者只统计已通过闸门。系统会针对模糊成功标准、先例不足、缺少推进记录和未写卡点给出提示，但不会替用户宣称课题成功。当前闸门可以生成一份三日计划并进入既有计划导入链。

联网检索仅在用户明确点击后调用 Crossref 固定地址。检索词只作为 URL 编码查询参数，不能选择任意目标主机；保存的是公开元数据和 DOI/来源链接，不自动下载论文全文。

## 5. 动态个人工作区

`workspaces` 保存稳定 `workspace_key`、显示名称、图标、模块类型、排序、启用状态、首页固定、当前目标、流程和组件组合。默认提供六个工作区，但用户可以改名、换色、排序、停用、调整证据流程与常用组件，或新建：

- `knowledge`：通用知识专题；
- `experiments`：结构化实验台账；
- `simulations`：可复现模拟案例；
- `datasets`：数据集档案。
- `ml`：数据、特征、代码、模型卡和验证结论；
- `md`：结构、力场、输入、日志、轨迹和分析；
- `comsol`：物理问题、几何材料、网格求解和实验校核。

默认稳定键为 `eg-lab`、`lammps-lab`、`dataset-lab`、`ml-lab`、`md-lab` 与 `comsol-lab`。升级只补齐缺失键，不覆盖已有工作区的个性化名称、顺序、启用状态、目标、流程、组件或首页固定。

不同模块的推荐搭配不共用一张空白模板：

- ML：数据版本与拆分、特征管线、参数与代码版本、指标、产物、误差和外部验证；
- MD：初始结构、力场与电荷、最小化/平衡、生产轨迹和分析脚本；
- LAMMPS：input/data、势函数来源、程序版本、运行命令、log/dump/restart 与通过标准；
- 数据集：来源与许可、字段单位、清洗版本、质量边界和训练/验证拆分；
- COMSOL：方程假设、几何材料、边界条件、网格无关性、求解器与实验校核；
- EG 实验：样品编号、配方与过程、原始曲线、异常、重复性和结论边界。

科研记录通过 `workspace_id` 归属工作区。停用定义不会级联删除资料。

## 6. 交付与复盘来源

### 6.1 交付

`mission_deliveries` 保存任务交付元数据，附件保存在：

```text
storage/deliveries/<storage_key>/
```

如果填写复盘关键文本，系统同时写入：

```text
复盘关键文本.txt
```

### 6.2 统一复盘来源

`review_sources` 是每日任务与特殊任务的共同索引：

| 字段 | 作用 |
|---|---|
| `source_type` | `mission_delivery` 或 `special_task` |
| `source_id` | 原始交付 ID |
| `source_text` | 受控关键文本 |
| `storage_key` | 可追溯交付目录 |
| `source_date` | 延迟复盘日期索引 |

AI 不会自动读取整个科研库。只有用户主动写入交付目录的关键文本才能进入自动题库。

## 7. 出题与开放题评分

### 7.1 出题

`services/review_engine.py` 要求模型返回固定结构：

```json
{
  "questions": [
    {
      "question": "...",
      "evidence": "...",
      "rubric": ["...", "..."],
      "kind": "recall"
    }
  ]
}
```

问题、证据与评分点一起写入 `review_sessions`。用户答题前不显示证据。

### 7.2 评分

开放答案不采用字符串相等和单一正确答案。评分输出为：

- `score`：0–100 的形成性分数；
- `level`：掌握、部分掌握、需要复习；
- `feedback`：缺失要素与下一步；
- `evidence_quote`：依据的交付证据；
- `confidence`：模型判断置信度。

之后用户必须选择“掌握 / 模糊 / 忘了”。`next_due` 由本人判断决定，而不是让模型成为最终裁判。

外部模型失败时，系统使用低置信度离线初判，并明确显示回退原因。

## 8. AI 提供器与安全边界

`services/ai_provider.py` 支持：

- 离线规则；
- Ollama `/api/generate`；
- OpenAI Responses API 或同结构兼容接口。

模型输出必须符合 JSON Schema。来自交付的文本被明确视为“不可信数据而非指令”，降低文档提示注入风险。

API 密钥只读取环境变量：

```text
OPENAI_API_KEY
RESEARCH_OS_AI_KEY
```

密钥不进入 `settings`、备份 JSON、个性化包或 Git 仓库。

## 9. 灵草、炼丹、秘境与雷劫

### 9.1 特殊任务

`special_tasks` 保存：

- 可检查任务；
- 难度 1–5；
- 最小交付；
- 交付说明；
- 复盘关键文本；
- 对应灵草奖励。

### 9.2 灵草

`herb_inventory` 以难度映射五级灵草。完成任务才增加库存。

### 9.3 丹药

丹药复用 `inventory_items`，`item_type='pill'`。万象秘境从复盘页独立出来。雷劫只在当前境界至少为金丹后期、累计修为达到下一大境界门槛且该关口尚未通过时开启；渡劫丹在开始五问雷劫时消耗，均分达到 70 分才在 `realm_tribulations` 中记录为通过。题目仍来自 `review_sources`。

游戏资产不能兑换真实权益，不能替代科研评价。

## 10. 个性化与跨版本迁移

个性化 v7 包包含：

- 个人主页；
- 字印头像或压缩后的头像图片；
- 主题、主页短句与每日诗句池；
- 网站名称；
- 39 阶稳定键境界名称；
- 全部导航名称、分组顺序、条目顺序与显示状态；
- 工作区定义、首页固定、目标、色调、流程与组件组合；
- 复盘弹窗开关。

不包含：

- 科研附件；
- 数据库；
- API 密钥；
- 联机 Token。

导入同时兼容 `research-cultivation-personalization-v1` 至 `v7`。v1.4 的 31 阶自定义名称会迁移到最接近的新稳定键；未修改的旧默认名自动替换为当前名称。v1–v6 没有布局字段时使用默认顺序。安全升级会迁移 `storage/profile/`，因此头像不会因版本更新丢失。

## 11. 轻量山门、检索与闭关

- 首页默认隐藏侧边栏，恢复山门、诗句、头像和四个核心入口；桌面常见尺寸优先完整呈现，窄屏允许自然滚动；
- 首页只保留一条双检索表单，同一个 `q` 参数可分别提交到 SQLite FTS5 和 Crossref 页面；
- 首页底部保持低密度工作台，最多显示本人固定的 6 个工作区，并提供全部工作区入口和本地/联机保护状态；
- 工作台下方只补最近保存、可用工作区数、数据位置与版本状态，不恢复高密度任务仪表盘；
- 首页加载不调用 Crossref，也不聚合论文轮播、每日一问、七日节律、今日奖励或工作区卡片；
- 今日主线、计划交付、论文管理、快速记录、成长反馈和备份仍由各自页面负责，首页不重复渲染；
- `POST /quick-capture` 仅为旧版兼容端点，不再作为首页入口；
- 桌面侧栏按“今日修炼、知识与交付、我的工作区、秘境与成长、协作与系统”分组并全部展开；移动端仍使用抽屉；
- 搜索继续使用 SQLite FTS5，中文无结果时回退到多关键词模糊匹配；
- 闭关页只把结束时间、剩余时间、目标和预设保存在浏览器 `localStorage`；
- 倒计时每次按 `Date.now()` 计算，不依赖后台标签页中的计时回调准时执行；
- 动效遵守 `prefers-reduced-motion`，系统要求减少动态时会自动关闭装饰动画。

## 12. 开放格式知识库导出

`/knowledge/export` 生成：

```text
README.md
manifest.json
knowledge.json
entries/*.md
attachments/*
records/experiments.json
records/simulations.json
records/review_sources.json
records/research_projects.json
records/project_milestones.json
records/project_cases.json
records/project_updates.json
records/workspaces.json
```

该导出用于阅读、二次分析和迁往其他知识管理工具，不承担整套系统恢复。数据库、交付目录和个性化的完整恢复继续由完整备份负责。

## 13. 数据安全

- SQLite：WAL、外键、忙等待；
- 自动快照：Python `sqlite3.Connection.backup()`；
- 完整备份：数据库与 `storage/` 一起打包；
- 安全升级：校验 ZIP 路径、安装到新目录、迁移数据、保留旧目录；
- 公开仓库：忽略数据库、附件、`.env`、Token、密钥与生成包。

正式 Windows 发行采用 PyInstaller one-file 结构：GitHub Release 直接提供两个可双击的独立程序。`ResearchOS.exe` 是每位用户的个人科研节点，普通模式把数据写入 `%LOCALAPPDATA%\ResearchCultivationOS`；`ResearchHub.exe` 是小团队管理员才需要运行的轻量中心，把账号、邀请码与中心状态写入 `%LOCALAPPDATA%\ResearchCultivationOSHub`。两个程序的数据根目录互不复用，EXE 更新或临时展开目录变化不会触碰数据库与附件。

便携 ZIP 同时提供个人节点、联机中心和双端同机启动脚本；个人便携数据位于 `user_data/`，中心便携数据位于 `hub_data/`。源码模式继续使用仓库内的 `instance/` 与 `storage/`，以兼容既有开发流程。

更新边界固定为：

```text
可替换：ResearchOS.exe、ResearchHub.exe、版本说明
必须保留：instance/、storage/、user_config/、user_data/、hub_data/
```

GitHub `main` 只承载通过回归的公开稳定源码，GitHub Releases 以两个 Windows EXE 为首要下载并保留便携 ZIP；未完成开发保存在另一个本地工作树或私有仓库，不推送到公开分支。每次正式发布同时在公开与私有仓库建立不可复用的 `release/vX.Y.Z` 快照分支；公开仓库中的任何分支都不能承担“隐藏开发源码”的职责。

轻量中心首次初始化只建立一个管理员。一次性凭据只写入主机 `instance/HUB_ADMIN_CREDENTIALS.txt`，被 Git、便携包和安全升级排除；管理员首次修改密码后该文件自动销毁。成员注册固定为 `member`，网页没有自助提权入口。

## 14. 后续扩展边界

建议下一阶段优先做：

1. 为课题先例增加可选的机构文献服务接入，但仍不把全文自动上传；
2. 将本人自评真正用于到期题抽样；
3. 增加交付关键文本的人工编辑与版本记录；
4. 为 AI 调用增加可见的费用上限和调用日志；
5. 增加浏览器端端到端测试。

暂不建议：

- 自动扫描全部私人文件；
- 自建大型向量数据库；
- 将科研原始数据同步到轻量中心；
- 让模型评分直接决定高风险学术或实验决策。
